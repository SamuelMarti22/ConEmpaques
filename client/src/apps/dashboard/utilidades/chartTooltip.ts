import type { CSSProperties } from 'react';

/** Estilo unificado de tooltips Recharts en el dashboard (usabilidad). */
export const estiloContenedorTooltip: CSSProperties = {
  backgroundColor: 'rgba(255, 255, 255, 0.97)',
  border: '1px solid rgba(18, 64, 55, 0.18)',
  borderRadius: 8,
  boxShadow: '0 8px 24px rgba(16, 41, 36, 0.12)',
  fontSize: 13,
};
